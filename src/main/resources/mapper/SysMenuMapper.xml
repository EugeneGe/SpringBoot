<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gly.springboot.mapper.SysMenuMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.gly.springboot.entity.sys.SysMenu">
        <id column="menu_id" property="menuId" />
        <result column="menu_name" property="menuName"/>
        <result column="parent_id" property="parentId"/>
        <result column="order_num" property="orderNum"/>
        <result column="path" property="path"/>
        <result column="component" property="component"/>
        <result column="query" property="query"/>
        <result column="is_frame" property="isFrame"/>
        <result column="is_cache" property="isCache"/>
        <result column="menu_type" property="menuType"/>
        <result column="visible" property="visible"/>
        <result column="status" property="status"/>
        <result column="perms" property="perms"/>
        <result column="icon" property="icon"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="remark" property="remark"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_List">
        menu_id, menu_name, parent_id, order_num, path, component, query, is_frame, is_cache, menu_type, visible, status, perms, icon, create_by, create_time, update_by, update_time, remark
    </sql>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        menu_id AS menuId,menu_name AS menuName,parent_id AS parentId,order_num AS orderNum,path,component,query,is_frame AS isFrame,is_cache AS isCache,menu_type AS menuType,visible,status,perms,icon,create_by AS createBy,create_time AS createTime,update_by AS updateBy,update_time AS updateTime,remark
    </sql>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List_2">
            u.menu_id AS menuId,
            u.menu_name AS menuName,
            u.parent_id AS parentId,
            u.order_num AS orderNum,
            u.path AS path,
            u.component AS component,
            u.query AS query,
            u.is_frame AS isFrame,
            u.is_cache AS isCache,
            u.menu_type AS menuType,
            u.visible AS visible,
            u.status AS status,
            u.perms AS perms,
            u.icon AS icon,
            u.create_by AS createBy,
            u.create_time AS createTime,
            u.update_by AS updateBy,
            u.update_time AS updateTime,
            u.remark AS remark,
    </sql>

    <!-- 通用条件列 -->
    <sql id="SysMenuByCondition">
        <if test="menuName != null and menuName != ''">
            AND menu_name = #{menuName}
        </if>
        <if test="parentId != null and parentId != ''">
            AND parent_id = #{parentId}
        </if>
        <if test="orderNum != null and orderNum != ''">
            AND order_num = #{orderNum}
        </if>
        <if test="path != null and path != ''">
            AND path = #{path}
        </if>
        <if test="component != null and component != ''">
            AND component = #{component}
        </if>
        <if test="query != null and query != ''">
            AND query = #{query}
        </if>
        <if test="isFrame != null and isFrame != ''">
            AND is_frame = #{isFrame}
        </if>
        <if test="isCache != null and isCache != ''">
            AND is_cache = #{isCache}
        </if>
        <if test="menuType != null and menuType != ''">
            AND menu_type = #{menuType}
        </if>
        <if test="visible != null and visible != ''">
            AND visible = #{visible}
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="perms != null and perms != ''">
            AND perms = #{perms}
        </if>
        <if test="icon != null and icon != ''">
            AND icon = #{icon}
        </if>
        <if test="createBy != null and createBy != ''">
            AND create_by = #{createBy}
        </if>
        <if test="createTime != null and createTime != ''">
            AND create_time <![CDATA[ = ]]>  #{createTime}
        </if>
        <if test="updateBy != null and updateBy != ''">
            AND update_by = #{updateBy}
        </if>
        <if test="updateTime != null and updateTime != ''">
            AND update_time <![CDATA[ = ]]>  #{updateTime}
        </if>
        <if test="remark != null and remark != ''">
            AND remark = #{remark}
        </if>
        <if test="list != null and list.size() > 0">
            AND id IN
            <foreach collection="list" separator="," item="item" index="index" open="(" close=")">
                #{item.id}
            </foreach>
        </if>
    </sql>


    <!-- 根据主键查询表sys_menu信息 -->
    <select id="selectMapById" resultType="java.util.Map">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_menu
        WHERE
            menu_id = #{id}
    </select>

    <!-- 根据条件查询表sys_menu信息 -->
    <select id="selectListByMap" resultType="java.util.Map">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_menu
    <where>
        <include refid="SysMenuByCondition" />
    </where>
    </select>

    <!--检验重复性-->
    <select id="checkRepetition" parameterType="java.util.Map" resultType="java.util.Map">
        SELECT
        id
        FROM sys_menu
    <where>
        <if test="id != null and id != ''">
            AND id != #{id}
        </if>
        <include refid="SysMenuByCondition" />
    </where>
    </select>



</mapper>
